interface:
  display_name: "Pmm Messaging Positioning"
  short_description: "Build positioning, value propositions, message houses, persona messaging, names, taglines, and launch narratives."
  brand_color: "#111827"
policy:
  allow_implicit_invocation: true
dependencies:
  tools:
    - type: "mcp"
      value: "marketing-os"
      description: "Bundled read-only MCP server for PMM resources and workflow routing."
      transport: "stdio"
